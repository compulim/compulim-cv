apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: server
spec:
  replicas: 2
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: server
    spec:
      containers:
      - name: server
        image: compulim.azurecr.io/server:latest
        ports:
        - containerPort: 80
      - name: app-compulim-info
        image: compulim.azurecr.io/app-compulim-info:latest
        ports:
        - containerPort: 4001
---
apiVersion: v1
kind: Service
metadata:
  name: server
spec:
  type: LoadBalancer
  ports:
  - port: 80
  selector:
    app: server
