{
  "name": "root",
  "version": "0.0.0-0",
  "private": true,
  "devDependencies": {
    "lerna": "^5.5.4"
  },
  "scripts": {
    "build": "cd packages && cd app-compulim-info && npm run build",
    "prepare": "lerna bootstrap"
  }
}
